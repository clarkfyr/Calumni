%li
  - if current_page.first?
    = link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :remote => "false", :id=>"pagination_grey"
  - else
    = link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :remote => remote
