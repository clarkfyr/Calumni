%li
  - if current_page.last?
    = link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote, :id=>"pagination_grey"
  - else
    = link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote

